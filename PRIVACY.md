# プライバシーポリシー

Illustration Archiveは、ユーザーがブックマークしたpixiv作品のメタデータと画像を、ユーザー自身が選択したローカルフォルダーへ保存します。

## 取得・保存する情報

- pixivの作品ID、タイトル、作者名、作者ID、タグ、説明、投稿日、元URL
- 作品画像
- 拡張機能のテーマ設定、利用上の注意と自動保存の同意状態、保存先フォルダーへのアクセスハンドル

作品画像は選択されたフォルダーへ保存され、一覧表示に必要なメタデータはブラウザ内へ保存されます。

## 外部送信

本拡張機能は独自の外部サーバーや解析サービスを使用しません。作品情報と画像を第三者へ送信しません。ユーザーが「Googleで検索」を選んだ場合に限り、作品ID、タイトル、作者名を含む検索語がGoogleへ送信されます。

## 権限

- `storage`: テーマ設定、保存先識別情報、作品一覧のメタデータを保存するため
- `scripting`: 拡張機能更新時、開いているpixiv作品ページへ自動保存処理を接続するため
- `declarativeNetRequestWithHostAccess`: pixiv画像サーバーから作品画像を取得するために必要なRefererを設定するため
- `www.pixiv.net` / `i.pximg.net`: 作品情報と作品画像を取得するため

## データの削除

保存一覧の削除操作はブラウザ内の一覧データを削除します。外部フォルダーへ保存された画像は誤削除防止のため自動削除せず、ユーザーがファイル管理アプリから削除します。
